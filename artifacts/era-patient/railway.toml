[build]
buildCommand = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @workspace/era-patient run build"

[deploy]
startCommand = "node server.cjs"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
