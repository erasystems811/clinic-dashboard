[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/era-patient run build"

[deploy]
startCommand = "pnpm --filter @workspace/era-patient run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
